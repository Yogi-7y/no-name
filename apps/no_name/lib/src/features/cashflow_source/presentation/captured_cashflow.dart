import 'package:flutter/material.dart';

class CapturedCashflow extends StatelessWidget {
  const CapturedCashflow({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('Captured');
  }
}
